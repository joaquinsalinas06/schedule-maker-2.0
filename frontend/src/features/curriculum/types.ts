// Domain types for the curriculum feature.
//
// ponytail: src/types/curriculum.ts is not owned by this migration (other
// files outside this feature import from it directly) so it's kept in place
// as the canonical source and re-exported here rather than duplicated/moved.
export * from '@/types/curriculum'
